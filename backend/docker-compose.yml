version: '3.8'
services:
  iot-backend:
    build: .
    container_name: iot-backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app
